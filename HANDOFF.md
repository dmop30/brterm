# 引き継ぎメモ

> セッションを跨ぐときの唯一の入口。**作業を始める前にここを読む。**
> web のコンテナは使い捨てで、push していないものは残らない。

最終更新: 2026-09-13

## どこに何があるか

| 知りたいこと                                          | 見る場所                                                                          |
| ----------------------------------------------------- | --------------------------------------------------------------------------------- |
| **いま何が動いているか**（Issue・PR・依存・マージ順） | [Issue #2](https://github.com/dmop30/brterm/issues/2)。**動くたびに更新している** |
| 仕様                                                  | `docs/requirements.md`（版 1.0。利用者から受領）                                  |
| 画面設計                                              | `docs/design/screen-design-v1.md`（確定済み）                                     |
| **決まったこと・実測・待ちごと**                      | この文書                                                                          |

**この文書は PR ごとには更新しない。** 並列で PR を出すため、全員が同じ 1 ファイルを
書き換えると衝突する。進捗は Issue #2 に置き、ここは**セッションの区切り**と
**大きな判断が確定したとき**に、専用のブランチで更新する。

## 現在地

要件定義を Issue に分解し、**端末が動くところまで到達した**。

| main に入ったもの                                      | Issue |
| ------------------------------------------------------ | ----- |
| 開発環境の土台（hook・CI・Makefile・VSCode）           | —     |
| 画面シェル（左レール・タブ・ステータスバー・2 テーマ） | #11   |
| ワークスペースの画面分割（縦・横）                     | #28   |
| 保存層と暗号（store / crypto / スキーマ v1）           | #3    |

出している PR とその積み方は Issue #2 の表を見る。

## 次の一手

1. 積んである PR を **土台から順にマージ**する（#26 → #27 → #33 → #34）
2. マージ後、`main` から **#13（SFTP 基本）** と **#14（履歴・手順書・危険コマンド確認）** に着手。
   どちらも #10 / #5 の上に乗るが、触るファイルが違うので並行できる
3. 続けて **#15（プロファイル・鍵・予定実行）**、**#19（ファイルツリー + ブックマークの画面）**

## 決まったこと

- **画面設計 v1 を採用**（配色・字の大きさ・状態の見せ方・確認ダイアログの体裁）
- **ショートカットは Alt 既定。** Chrome は `Ctrl+T` / `Ctrl+W` / `Ctrl+Tab` を画面に渡さない
- **1 Issue = 1 PR。** ブランチは `claude/issue-<番号>-<短い名前>`
- **PR は積む（stacked）。** 依存する PR の上にブランチを作り、土台がマージされたら base を `main` へ向け直す
- **実装を壊すとテストが落ちることを確かめてから PR にする**（変異を入れて確認する）
- GUI を含む Issue は、実装後に**利用者へ動作確認を依頼**する

## 待っていること

|                | 内容                                                                                                                                      |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| **別紙が無い** | `docs/requirements-resume-and-passkey.md`（機能 A / 機能 B の詳細）が未受領。#17（パスキー）と #18（セッション再開 L2〜L4）は着手できない |
| 既存実装       | 要件定義 2 章は多くを「実装済み」としているが、このリポジトリには無かった。**P0 として作り直している**（利用者の判断で決定済み）          |

## 実測メモ

**道具まわり**

- `node --test` のグロブ展開は **Node 22 では効くが 20.11 では効かない**。`scripts/run-tests.mjs` で自分で集める。CI は 20.11 と 22 の両方で回す
- `node --test` の 1 件あたりの上限は**既定が無制限**。繋ぎっぱなしの試験が CI を止めるため 30 秒の上限を入れた
- 試験のあとに **SSH の接続を閉じないと試験ファイルが終わらない**（全部通っているのに時間切れになる）
- `ssh2` にはサーバ実装が入っている。**試験の中で実際に SSH サーバを立てて**接続まで確かめられる（依存の追加なし）
- `ssh2` は CommonJS。`import { Server } from 'ssh2'` では取り出せないものがあるので、既定 import から分解する
- `npm install` はビルドツール無しで完了する。`ssh2` の optional 依存 `cpu-features` は、ビルドツールがある環境でだけネイティブ拡張を作る（無くても動く）
- eslint 10 の依存は Node 20.19+ を要求するため、Node 20.11 の CI では EBADENGINE 警告が出る。検査自体は通る

**画面まわり（実機で踏んだもの。試験では出なかった）**

- **親から渡る関数を effect の依存に入れない。** 描画のたびに別物になるため、WebSocket を張り直し続ける無限ループになる。控え（ref）に持たせる
- **`ResizeObserver` + `fit` は帰還ループを作る。** xterm を器から絶対配置で切り離し、桁・行が変わったときだけ `resize` を送る
- **xterm は押鍵をほぼ全部シェルへ送る。** 画面側のショートカットは `attachCustomKeyEventHandler` で通す
- 面（画面分割）の境界は **grid のトラックとして数える**。数えないと面の割合がずれる
- **画面の確認は実機で行う。** 上の 4 つはすべて、単体試験が全部緑のまま壊れていた

## 動かし方（確認用）

```bash
make check                    # typecheck + lint + 整形確認 + テスト
npm run build && npm start    # 出てくる URL（?token=…）を開く
```
