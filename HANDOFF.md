# 引き継ぎメモ

> セッションを跨ぐときの唯一の入口。**作業を始める前にここを読む。**
> web のコンテナは使い捨てで、push していないものは残らない。

## 現在地

**初期準備(開発環境の土台)が入った。機能の実装はまだ 1 つも入っていない。**

- 仕様書は `docs/requirements.md`(利用者から受領した版 1.0)に置いた
- `make check`(typecheck + lint + 整形確認 + テスト)が通る状態にした
- `npm run build` → `npm start` で `/api/health` と画面が出るところまで確認した
- Claude Code on the web の SessionStart hook を入れ、**その場で 3 点検証した**
  (版表示・2 回目も同じ結果・手元では何も出さない)

## 次の一手

`docs/requirements.md` 9 章の**フェーズ 1「土台の整理」**から。

- [ ] `src/shared/types.ts` に追加型(`ResumeConfig`、`FileTab`、`ProcedureStep.kind` など)を定義
- [ ] `src/server/vault.ts` を DEK / KEK 方式で新設(`local` モードで現行と同じ結果になること)
- [ ] `store.ts` のスキーマ版数を 2 にし、旧 `db.json` からの移行処理を入れる

**フェーズごとに 1 PR・コミットを分ける。** どのフェーズでも `npm run typecheck` を完了条件に含める。

## 未解決・確認が要ること

|                    | 内容                                                                                                                                                                                                                                                                                                 |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **既存実装が無い** | 要件定義 2 章は `src/server/{index,api,ws,ssh,sftp,store,crypto,keys,scheduler,paths}.ts` と `src/client/*` を「実装済み」としているが、**このリポジトリには入っていない**(初期コミットは README のみ)。別の場所にある実装を持ち込むのか、フェーズ 1 の前に 2 章の機能から作るのかを決める必要がある |
| **別紙が無い**     | `docs/requirements-resume-and-passkey.md`(機能 A / 機能 B の詳細)が未受領。フェーズ 2〜4 はこれが無いと詳細を詰められない                                                                                                                                                                            |
| 骨組みの扱い       | `src/server/index.ts` は `/api/health` と静的配信だけの仮。トークン認証・Origin 検証は未実装。実装時に置き換える                                                                                                                                                                                     |

## 入れた開発ツール(要件定義 4.10 の一覧外。理由つき)

4.10 はアプリの依存の一覧なので、検査・整形・テストの道具は別途入れた。いずれも純 JS。

|                                                                                         | 理由                                                                                          |
| --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| `typescript` / `@types/*`                                                               | 型検査。`npm run typecheck` が完了条件に入っているため                                        |
| `vite` / `@vitejs/plugin-react`                                                         | 要件定義 2 章の技術スタックどおり                                                             |
| `eslint` / `typescript-eslint` / `@eslint/js` / `globals` / `eslint-plugin-react-hooks` | 静的検査。CI と hook の入口を揃えるため                                                       |
| `prettier`                                                                              | 整形。手で整形しないため                                                                      |
| `tsx`                                                                                   | `node --test` で TypeScript のテストをそのまま動かすため(要件定義 10 章が `node --test` 指定) |
| `concurrently`                                                                          | `npm run dev` でサーバと Vite を同時に出すため                                                |

## 実測メモ

- `npm install` は**ビルドツール無しで完了する**(要件定義 7 章)。
  ただし `ssh2` の optional 依存 `cpu-features` は、ビルドツールがある環境では
  ネイティブ拡張(`sshcrypto.node`)を作る。**無くても動く**(純 JS 実装に落ちる)ので方針違反ではない。
  ビルドツールが無い環境では警告が出るだけ。
- `cron-parser` は v4 が保守終了(`npm install` が deprecated 警告を出す)のため **v5** を入れた。
  v5 は API が `CronExpressionParser.parse()` に変わっている。予定実行の実装時に注意。
- SessionStart hook は毎回 `npm ci` を走らせる(約 6 秒)。同期実行のままにしてある。
