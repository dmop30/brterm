# 引き継ぎメモ

> セッションを跨ぐときの唯一の入口。**作業を始める前にここを読む。**
> web のコンテナは使い捨てで、push していないものは残らない。

最終更新: 2026-09-13

## 現在地

**要件定義（`docs/requirements.md` 版 1.0）を Issue に分解し、土台から実装している。**
全体の地図は [Issue #2](https://github.com/dmop30/brterm/issues/2)。段階は P0（要件定義 2 章の機能を作る）→ P1〜P9（9 章のフェーズ）。

| 入ったもの（main）                                     | Issue |
| ------------------------------------------------------ | ----- |
| 開発環境の土台（hook・CI・Makefile・VSCode）           | —     |
| 画面シェル（左レール・タブ・ステータスバー・2 テーマ） | #11   |

| 出している PR | Issue | 内容                                              |
| ------------- | ----- | ------------------------------------------------- |
| #24           | #3    | 保存層と暗号（store / crypto / 型 / スキーマ v1） |
| #26           | #4    | HTTP API・トークン認証・Origin 検証（#24 に積載） |
| #27           | #5    | SSH セッション管理（#24 に積載）                  |
| #30           | #28   | ワークスペースの画面分割（縦・横）                |
| #31           | #29   | ディレクトリのブックマーク（#26 に積載）          |

**PR は積んである。** 土台からマージすると、上の PR の base は向き直る（自動で戻らないときは手で base を `main` に変える）。

## 次の一手

1. #24 がマージされたら **#10（WebSocket 端末入出力と文字コード変換）**。#5 の `Session` に繋ぐ
2. 続けて **#12（端末画面 xterm.js）** → 端末が動く状態にする
3. 並行して **#13（SFTP 基本）**、**#14（履歴・手順書・危険コマンド確認）**、**#15（プロファイル・鍵・予定実行）**

## 決まっていること（判断の根拠）

- **画面設計は確定済み。** `docs/design/screen-design-v1.md`。配色・字の大きさ・状態の見せ方・確認ダイアログの体裁
- **ショートカットは Alt 既定**（#30 で変更、利用者確認待ち）。Chrome は `Ctrl+T` / `Ctrl+W` / `Ctrl+Tab` を画面に渡さない
- **1 Issue = 1 PR。** ブランチは `claude/issue-<番号>-<短い名前>`
- **実装を壊すとテストが落ちることを確かめてから PR にする**（変異を入れて確認する）
- GUI を含む Issue は、実装後に**利用者へ動作確認を依頼**する

## 待っていること

|                | 内容                                                                                                                                           |
| -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| **別紙が無い** | `docs/requirements-resume-and-passkey.md`（機能 A / 機能 B の詳細）が未受領。#17（パスキー）と #18（セッション再開）はこれが無いと着手できない |
| 既存実装       | 要件定義 2 章は多くを「実装済み」としているが、このリポジトリには無かった。**P0 として作り直している**（利用者の判断で決定済み）               |

## 実測メモ

- `node --test` のグロブ展開は **Node 22 では効くが 20.11 では効かない**。`scripts/run-tests.mjs` で自分で集める。CI は 20.11 と 22 の両方で回す
- `node --test` の 1 件あたりの上限は**既定が無制限**。繋ぎっぱなしの試験が CI を止めるため 30 秒の上限を入れた
- `ssh2` にはサーバ実装が入っている。**試験の中で実際に SSH サーバを立てて**接続まで確かめられる（依存の追加なし）
- `ssh2` は CommonJS。`import { Server } from 'ssh2'` では取り出せないものがあるので、既定 import から分解する
- 面（画面分割）の境界は **grid のトラックとして数える**。数えないと面の割合がずれる
- `npm install` はビルドツール無しで完了する。`ssh2` の optional 依存 `cpu-features` は、ビルドツールがある環境でだけネイティブ拡張を作る（無くても動く）
- eslint 10 の依存は Node 20.19+ を要求するため、Node 20.11 の CI では EBADENGINE 警告が出る。検査自体は通る
