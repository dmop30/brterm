# brterm — Claude 向け運用ルール

## このリポジトリの目的

ブラウザで動く **SSH / SFTP ターミナル**。Windows・macOS・Linux で同じように動く。
想定利用者はサーバの保守・運用を手作業で行う技術者で、
ターミナル・SFTP クライアント・エディタ・Wiki の往復を 1 画面に畳み込むことを目的にする。

成果物は npm パッケージ(`brterm` コマンド)と Electron アプリ。

## やり取りの言語

日本語。画面文言とエラーも日本語(命令形・能動態で短く)。サーバログは英語可。

## セッション開始時にまずやること

```bash
cat HANDOFF.md      # 現在地と次の一手
cat docs/requirements.md  # 単一の仕様書。実装順と受け入れ条件はここ
make check          # typecheck + lint + 整形確認 + テストが通る状態から始める
```

**web のセッションは使い捨てである。** push していないものは残らない。
実装は `docs/requirements.md` 9 章のフェーズ順に進め、**フェーズごとにコミットを分ける**。

## 開発時のチェック

```bash
make setup      # 依存導入(SessionStart hook が web セッション開始時に自動で走らせる)
make check      # typecheck + lint + fmt-check + test
make dev        # サーバ :7781 + Vite :5273
make build      # dist/server と dist/client
make start      # ビルド済みを起動
```

`npm run typecheck` は **PR 前に必ず通す**(要件定義 9 章の完了条件)。
コードを変更したらテストを追加する。**「テストが通る」だけを条件にしない** —
実装を壊したときに落ちるかまで確かめる。

## 守ること

1. **ネイティブ依存を増やさない。** `npm install` にビルドツールを要求しない。
   `node-pty` / `keytar` / `argon2` は採用しない。依存追加は要件定義 4.10 の一覧に限り、
   それ以外が必要になったら理由を添えて提案する。
2. **既定はオフ。** 新機能は設定で有効化する。既存利用者の体験と保存データを壊さない。
3. **安全側に倒す。** 復号できない・再接続できない・権限が足りない状況では、
   推測で続行せず状態を明示して止める。
4. **破壊的操作は必ず確認。** 確認ダイアログの文言に**対象のパスまたはホスト名を必ず含める**。
5. **秘密情報をログに出さない。** 既定は 127.0.0.1 待ち受け、トークン認証と Origin 検証。
6. 画面の文言は要件定義の表現を使う。**新しい語彙を勝手に増やさない**(用語は要件定義 3 章)。
7. **検証していないものを「できた」と言わない。** 未実施は未実施と書く。
8. **都合の悪い結果を薄く書かない。**
9. 不明点があれば実装を止めて質問する。**推測で仕様を決めない。**

## コードの書き方

|             |                                                                                                    |
| ----------- | -------------------------------------------------------------------------------------------------- |
| 型の共有    | `src/shared` 経由。サーバ・画面のどちらかしか使わない型は置かない                                  |
| 相対 import | **サーバ側(`src/server`, `src/shared`)は `.js` 拡張子つき**(NodeNext)。画面側は拡張子なし(Bundler) |
| コメント    | 日本語。**なぜそう書いたか**を書く。何をしているかはコードで読ませる                               |
| 整形        | prettier(`make fmt`)。手で整形しない                                                               |

## 環境

|        |                                                                          |
| ------ | ------------------------------------------------------------------------ |
| 言語   | TypeScript 5 / Node.js 20.11+                                            |
| サーバ | Express 4 / ws / ssh2 / iconv-lite / cron-parser                         |
| 画面   | React 18 / Vite 5 / @xterm/xterm / lucide-react                          |
| 検査   | tsc(3 プロジェクト) / eslint 10 / prettier / `node --test`(tsx 経由)     |
| 起動   | `.claude/hooks/session-start.sh` が web セッション開始時に `npm ci` する |
| 保存先 | `~/.brterm`(`BRTERM_DATA_DIR` で変更可)                                  |

`tsconfig` は 3 つに分かれている。**編集するファイルに対応するものを見る。**

| 置き場所                   | 設定                       | 解決方式               |
| -------------------------- | -------------------------- | ---------------------- |
| `src/client`, `src/shared` | `tsconfig.json`            | Bundler                |
| `src/server`, `src/shared` | `src/server/tsconfig.json` | NodeNext(出力 `dist/`) |
| `tests`                    | `tests/tsconfig.json`      | NodeNext(出力なし)     |

## GitHub Actions

外部アクションは **必ず commit SHA(40桁)で固定し、`# vX.Y.Z` のコメントを残す**。
タグは付け替え可能で、固定していないと差し替えで任意のコードが走る。
更新は Dependabot が cooldown 経過後に PR で提案する。手で最新へ上げない。
